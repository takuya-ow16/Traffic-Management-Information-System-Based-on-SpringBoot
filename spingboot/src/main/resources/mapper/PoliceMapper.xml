<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.PoliceMapper">
    <select id="selectAll" resultType="com.example.entity.Police">
        select * from police
        <where>
            <if test="name != null">
                police.name like concat('%',#{name},'%')
            </if>
        </where>
        order by police.id desc
    </select>

    <insert id="insert" parameterType="com.example.entity.Police">
        insert into police (username, password, name, id_card, phone, poid, avatar, role, status)
        values(#{username}, #{password}, #{name}, #{idCard}, #{phone}, #{poid}, #{avatar}, #{role}, #{status})
    </insert>

    <update id="updateById" parameterType="com.example.entity.Police">
        update police set username = #{username}, password = #{password}, name = #{name}, id_card = #{idCard}, phone = #{phone}, poid = #{poid}, avatar = #{avatar}, role = #{role}, status=#{status} where id = #{id}
    </update>


</mapper>